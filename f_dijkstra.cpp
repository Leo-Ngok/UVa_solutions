//dijkstra-4
#include <queue>
#include <limits.h> 
#include <cstdio>
#include <cstring> 
#define MAXN 10000

int dist[MAXN],n;
bool visited[MAXN];
//n is the number of vertices, s is the vertex
void dijkstra(){
	memset(visited,0,sizeof(visited));
	for(int k=1;k<=n;k++)dist[MAXN]=INT_MAX;
	dist[s]=0;
	for(int i=0;i<n-1;i++){
		
	}
}
int main(){
	scanf("%d",&n); 
}
